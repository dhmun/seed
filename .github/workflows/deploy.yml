# This workflow is disabled in favor of Vercel deployment
# name: Deploy to GitHub Pages

# on:
#   push:
#     branches: [ master ]
#   workflow_dispatch:

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: "pages"
#   cancel-in-progress: false

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
        
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#           cache: 'npm'
          
#       - name: Install dependencies
#         run: npm ci
        
#       - name: Build for GitHub Pages
#         run: |
#           GITHUB_PAGES=true npm run build
#         env:
#           NEXT_PUBLIC_SITE_URL: https://dhmun.github.io/seed
          
#       - name: Setup Pages
#         uses: actions/configure-pages@v4
        
#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./out

#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4

# ==================================================
# VERCEL DEPLOYMENT INSTRUCTIONS
# ==================================================
# 
# To deploy this project to Vercel:
# 
# 1. Sign up/Login to Vercel (https://vercel.com)
# 2. Connect your GitHub repository
# 3. Set the following environment variables in Vercel dashboard:
#    - SUPABASE_URL (if using real Supabase)
#    - SUPABASE_SERVICE_KEY (if using real Supabase)
#    - Any other custom environment variables
# 
# 4. Deploy! Vercel will automatically:
#    - Build your Next.js app with full SSR/API support
#    - Enable server functions and API routes
#    - Provide automatic deployments on every push
# 
# The vercel.json file is already configured in the project root.
# No additional configuration needed!